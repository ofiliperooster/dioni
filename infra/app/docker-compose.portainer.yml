version: "3.8"

services:
  web:
    image: ghcr.io/ofiliperooster/dioni:latest
    environment:
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_INTERNAL_URL: ${SUPABASE_INTERNAL_URL:-http://gr-supabase_api-gw:8000}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      INITIAL_ADMIN_EMAIL: ${INITIAL_ADMIN_EMAIL}
      INITIAL_ADMIN_PASSWORD: ${INITIAL_ADMIN_PASSWORD}
    networks:
      - network_swarm_public
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 45s
    deploy:
      mode: replicated
      replicas: 1
      update_config:
        order: start-first
        parallelism: 1
        delay: 10s
      rollback_config:
        order: stop-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 5
      labels:
        - traefik.enable=true
        - traefik.docker.network=network_swarm_public
        - traefik.http.routers.dioni.rule=Host(`dioni.kernhost.co`)
        - traefik.http.routers.dioni.entrypoints=websecure
        - traefik.http.routers.dioni.tls=true
        - traefik.http.routers.dioni.tls.certresolver=letsencryptresolver
        - traefik.http.routers.dioni.middlewares=dioni-security
        - traefik.http.middlewares.dioni-security.headers.stsSeconds=31536000
        - traefik.http.middlewares.dioni-security.headers.stsIncludeSubdomains=true
        - traefik.http.middlewares.dioni-security.headers.stsPreload=true
        - traefik.http.middlewares.dioni-security.headers.contentSecurityPolicy=upgrade-insecure-requests
        - traefik.http.services.dioni.loadbalancer.server.port=3000

networks:
  network_swarm_public:
    external: true
